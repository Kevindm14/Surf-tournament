<table>
  <tr>
    <th>Name<th>
    <th>email</th>
    <th>Actions</th>
  </tr>

  <% @users.each do |user| %>
    <tr>
      <td><%= user.name %><%= user.last_name %><td>
      <td><%= user.email %></td>
      <td><% if can? :update, user %><%= link_to 'Edit', edit_user_path(user) %><% end %></td>
      <td><% if can? :destroy, user %><%= link_to 'Delete', user, method: :delete, data: { confirm: '¿Are you sure?'} %><% end %></td>
    </tr>
  <% end %>
    <% if can? :create, @user %>
      <%= link_to 'Add Judge', new_user_path %>
    <% end %>
</table>
